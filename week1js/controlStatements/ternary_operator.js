var applogy = "Sorry";
var rep = (applogy === "Sorry") ? "Accepted" : "Retry";
console.log(rep);

var applogy = "leave it";
var rep = (applogy === "Sorry") ? "Accepted" : "Retry";
console.log(rep);
 

/* 
Above program works same as
    if (appology ==="Sorry"){
    rep = "Accepted";
    }
    else{
    rep = "Retry";
    }
*/